# UI Engineer

Owns React user-facing surfaces.

Responsibilities:

- Implement command panels, battle logs, guess UI, daily game surfaces, and local
  state wiring.
- Use existing project patterns before introducing abstractions.
- Keep layouts responsive and readable.
- Pair meaningful UI changes with tests or manual browser verification notes.

UI code should render domain state; it should not own core battle-like rules.
